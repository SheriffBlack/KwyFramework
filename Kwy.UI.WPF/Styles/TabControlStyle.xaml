<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="pack://application:,,,/Kwy.UI.WPF;component/Styles/BaseStyle.xaml" />
        <ResourceDictionary Source="pack://application:,,,/Kwy.UI.WPF;component/Styles/TextBlockStyle.xaml" />
    </ResourceDictionary.MergedDictionaries>

    <!--  TabControl 样式：确保内容区域占满  -->
    <Style TargetType="TabControl">
        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
        <Setter Property="VerticalContentAlignment" Value="Stretch" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TabControl">
                    <Grid HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>
                        <TabPanel Grid.Row="0" IsItemsHost="True" />
                        <ContentPresenter
                            Grid.Row="1"
                            HorizontalAlignment="Stretch"
                            VerticalAlignment="Stretch"
                            ContentSource="SelectedContent" />
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!--  TabItem 样式：参考 RadioButtonItemStyle  -->
    <Style BasedOn="{StaticResource ControlBaseStyle}" TargetType="TabItem">
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Height" Value="35" />
        <Setter Property="Background" Value="{DynamicResource ControlBackgroundBrush}" />
        <Setter Property="Foreground" Value="{DynamicResource ForegroundBrush}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TabItem">
                    <Border x:Name="PART_TabItemBorder" BorderThickness="0,1,0,0">
                        <Grid>
                            <!--  右侧分隔线（垂直分隔线）  -->
                            <Border
                                x:Name="PART_RightBorder"
                                Width="1"
                                HorizontalAlignment="Right"
                                VerticalAlignment="Stretch"
                                Background="{DynamicResource BorderBrush}"
                                Opacity="0.3" />
                            <!--  TabItem 样式：参考 RadioButtonItemStyle  -->
                            <Border
                                x:Name="PART_TabItemContentBorder"
                                Background="{TemplateBinding Background}"
                                BorderThickness="0">
                                <Grid
                                    x:Name="PART_ContentGrid"
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Center">
                                    <ContentPresenter
                                        x:Name="PART_HeaderContentPresenter"
                                        Margin="20,0,20,0"
                                        HorizontalAlignment="Center"
                                        VerticalAlignment="Center"
                                        ContentSource="Header"
                                        RecognizesAccessKey="True" />
                                </Grid>
                            </Border>
                        </Grid>
                    </Border>

                    <ControlTemplate.Triggers>
                        <!--  选中状态  -->
                        <Trigger Property="IsSelected" Value="True">
                            <!--  只设置 Header 内容的颜色，不影响内容区域  -->
                            <Setter Property="Foreground" Value="{DynamicResource ControlSelectedForegroundBrush}" />
                            <Setter TargetName="PART_HeaderContentPresenter" Property="TextElement.Foreground" Value="{DynamicResource ControlSelectedForegroundBrush}" />
                            <Setter TargetName="PART_TabItemContentBorder" Property="Background" Value="{DynamicResource ControlSelectedBackgroundBrush}" />
                            <Setter TargetName="PART_TabItemBorder" Property="BorderThickness" Value="0,2,0,0" />

                            <Setter TargetName="PART_TabItemBorder" Property="BorderBrush" Value="{DynamicResource PrimaryBrush}" />
                            <Setter TargetName="PART_ContentGrid" Property="Opacity" Value="1" />
                        </Trigger>

                        <!--  未选中状态  -->
                        <Trigger Property="IsSelected" Value="False">
                            <!--  恢复 Header 内容的默认颜色  -->
                            <Setter TargetName="PART_ContentGrid" Property="Opacity" Value="1" />
                        </Trigger>

                        <!--  鼠标悬停保持无阴影，避免二级导航视觉过重。  -->

                        <!--  禁用状态  -->
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.5" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>



