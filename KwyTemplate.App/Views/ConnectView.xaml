<UserControl
    x:Class="KwyTemplate.App.Views.ConnectView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:kwymvvm="http://schemas.kwy.com/mvvm"
    xmlns:kwyuiC="http://schemas.kwy.com/ui/components"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="720"
    d:DesignWidth="1280"
    kwymvvm:ViewModelLocator.AutoWireViewModel="True"
    mc:Ignorable="d">
    <UserControl.Resources>
        <Style x:Key="SystemPanelStyle" TargetType="Border">
            <Setter Property="Margin" Value="12" />
            <Setter Property="Padding" Value="18" />
            <Setter Property="Background" Value="{DynamicResource ControlBackgroundBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource ControlBorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="CornerRadius" Value="6" />
        </Style>

        <Style x:Key="ConfigTitleStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="20" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Foreground" Value="{DynamicResource ControlForegroundBrush}" />
        </Style>
    </UserControl.Resources>

    <Grid Background="{DynamicResource BackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Style="{StaticResource SystemPanelStyle}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="12" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <StackPanel>
                    <TextBlock Style="{StaticResource ConfigTitleStyle}" Text="{DynamicResource Connect.Title.SystemConfig}" />
                    <TextBlock
                        Margin="0,6,0,0"
                        Foreground="{DynamicResource ControlSecondaryForegroundBrush}"
                        Text="{DynamicResource Connect.Description}" />
                </StackPanel>

                <Button
                    Grid.Column="1"
                    Width="120"
                    Height="36"
                    Command="{Binding ReloadCommand}"
                    Content="{DynamicResource Connect.Action.Reload}" />
                <Button
                    Grid.Column="3"
                    Width="120"
                    Height="36"
                    Command="{Binding SaveCommand}"
                    Content="{DynamicResource Text.Apply}" />
            </Grid>
        </Border>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding ConfigSources}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <GroupBox
                            Margin="12"
                            Padding="18"
                            Header="{Binding Header}">
                            <ItemsControl ItemsSource="{Binding Sources}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <kwyuiC:KwyPropertyGrid Padding="0" Source="{Binding}" />
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </GroupBox>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <TextBlock
            Grid.Row="2"
            Margin="18,0,18,12"
            Foreground="{DynamicResource ControlSecondaryForegroundBrush}"
            Text="{Binding StatusMessage}"
            TextWrapping="Wrap" />
    </Grid>
</UserControl>


