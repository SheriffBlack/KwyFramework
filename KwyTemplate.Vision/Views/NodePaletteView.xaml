<UserControl
    x:Class="KwyTemplate.Vision.Views.NodePaletteView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:behaviors="clr-namespace:KwyTemplate.Vision.Behaviors"
    xmlns:viewmodel="clr-namespace:KwyTemplate.Vision.ViewModels"
    MinWidth="200"
    Background="{DynamicResource VisionViewBackgroundBrush}">

    <UserControl.Resources>
        <!--  节点条目样式  -->
        <Style x:Key="PaletteItemStyle" TargetType="ListBoxItem">
            <Setter Property="Background" Value="{DynamicResource TransparentBrush}" />
            <Setter Property="Foreground" Value="{DynamicResource PaletteFg}" />
            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
            <Setter Property="Padding" Value="10,7" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border
                            x:Name="Bd"
                            Margin="2,1"
                            Padding="{TemplateBinding Padding}"
                            Background="{TemplateBinding Background}"
                            CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{DynamicResource PaletteItemHover}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Background="{DynamicResource VisionPanelBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!--  搜索框  -->
        <Border
            Grid.Row="0"
            Margin="8,6"
            Background="{DynamicResource PaletteSearchBackground}"
            BorderBrush="{DynamicResource VisionPanelBorderBrush}"
            BorderThickness="1"
            CornerRadius="6">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <TextBlock
                    Grid.Column="0"
                    Margin="8,0,4,0"
                    VerticalAlignment="Center"
                    FontSize="12"
                    Foreground="{DynamicResource PaletteSubFg}"
                    Text="🔍" />
                <TextBox
                    Grid.Column="1"
                    Padding="4,6"
                    Background="{DynamicResource TransparentBrush}"
                    BorderThickness="0"
                    CaretBrush="{DynamicResource PaletteAccent}"
                    FontSize="12"
                    Foreground="{DynamicResource PaletteFg}"
                    Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}">
                    <TextBox.Style>
                        <Style TargetType="TextBox">
                            <Style.Resources>
                                <VisualBrush
                                    x:Key="WatermarkBrush"
                                    AlignmentX="Left"
                                    AlignmentY="Center"
                                    Stretch="None">
                                    <VisualBrush.Visual>
                                        <TextBlock
                                            Margin="2,0"
                                            FontSize="12"
                                            Foreground="{DynamicResource VisionWatermarkBrush}"
                                            Text="搜索节点…" />
                                    </VisualBrush.Visual>
                                </VisualBrush>
                            </Style.Resources>
                            <Style.Triggers>
                                <Trigger Property="Text" Value="">
                                    <Setter Property="Background" Value="{StaticResource WatermarkBrush}" />
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </TextBox.Style>
                </TextBox>
            </Grid>
        </Border>

        <CheckBox
            Grid.Row="1"
            Margin="10,0,8,6"
            VerticalAlignment="Center"
            Content="只看收藏"
            FontSize="11"
            Foreground="{DynamicResource PaletteFg}"
            IsChecked="{Binding ShowFavoritesOnly}" />

        <!--  分类列表  -->
        <ScrollViewer
            Grid.Row="2"
            behaviors:ScrollViewerMouseWheelBehavior.IsWheelScrollEnabled="True"
            VerticalScrollBarVisibility="Auto">
            <ItemsControl Margin="4,0" ItemsSource="{Binding Categories}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate DataType="{x:Type viewmodel:NodeCategoryViewModel}">
                        <Expander Margin="0,2" IsExpanded="{Binding IsExpanded}">
                            <Expander.Header>
                                <TextBlock
                                    FontSize="11"
                                    FontWeight="SemiBold"
                                    Foreground="{DynamicResource PaletteAccent}"
                                    Text="{Binding CategoryName}" />
                            </Expander.Header>
                            <Expander.Style>
                                <Style TargetType="Expander">
                                    <Setter Property="Background" Value="{DynamicResource TransparentBrush}" />
                                    <Setter Property="Foreground" Value="{DynamicResource PaletteFg}" />
                                </Style>
                            </Expander.Style>
                            <ListBox
                                behaviors:NodePaletteDragBehavior.IsEnabled="True"
                                Background="{DynamicResource TransparentBrush}"
                                BorderThickness="0"
                                ItemContainerStyle="{StaticResource PaletteItemStyle}"
                                ItemsSource="{Binding Items}"
                                ScrollViewer.CanContentScroll="True"
                                VirtualizingPanel.IsVirtualizing="True"
                                VirtualizingPanel.VirtualizationMode="Recycling">
                                <ListBox.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <VirtualizingStackPanel />
                                    </ItemsPanelTemplate>
                                </ListBox.ItemsPanel>
                                <ListBox.ItemTemplate>
                                    <DataTemplate DataType="{x:Type viewmodel:NodePaletteItemViewModel}">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="28" />
                                            </Grid.ColumnDefinitions>
                                            <StackPanel Grid.Column="0" Margin="0,0,8,0">
                                                <TextBlock
                                                    FontSize="12"
                                                    Foreground="{DynamicResource PaletteFg}"
                                                    Text="{Binding DisplayName}"
                                                    TextTrimming="CharacterEllipsis"
                                                    TextWrapping="NoWrap" />
                                                <TextBlock
                                                    MaxHeight="32"
                                                    Margin="0,1,0,0"
                                                    FontSize="10"
                                                    Foreground="{DynamicResource PaletteSubFg}"
                                                    Text="{Binding Description}"
                                                    TextTrimming="CharacterEllipsis"
                                                    TextWrapping="Wrap" />
                                            </StackPanel>
                                            <Button
                                                Grid.Column="1"
                                                Width="24"
                                                Height="24"
                                                Padding="0"
                                                HorizontalAlignment="Right"
                                                VerticalAlignment="Center"
                                                Background="{DynamicResource TransparentBrush}"
                                                BorderThickness="0"
                                                Command="{Binding DataContext.ToggleFavoriteCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                CommandParameter="{Binding}"
                                                Foreground="{DynamicResource PaletteAccent}"
                                                ToolTip="收藏节点">
                                                <TextBlock Text="{Binding FavoriteText}" />
                                            </Button>
                                        </Grid>
                                    </DataTemplate>
                                </ListBox.ItemTemplate>
                            </ListBox>
                        </Expander>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>
